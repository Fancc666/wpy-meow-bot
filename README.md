# wpy-meow-bot

微北洋自动回复评论的猫猫机器人。

## 使用

克隆本仓库。

将`.env.example`重命名为`.env`并正确填写环境变量。

如果在异地服务器接入学校AI接口，推荐使用`HeartBeat.py`使easyconnect保活。

安装依赖

```bash
pip install uv
uv sync
```

启动

```bash
uv run main.py
```

## 说明

请勿滥用接口，注意账号安全。
